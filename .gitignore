# Ignore everything
*
# Except the asset folder
!asset/
!asset/**
# But still ignore specific sensitive files
/Secret/.env
# Ignore the .env file in the Secret directory
/Secret/
# Ignore the entire Secret directory
js/config.js
# Ignore config file containing API keys
js/firebase-config.js
# Ignore Firebase configuration file